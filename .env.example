APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost/myapp/public

APP_LOCALE=fa
APP_FALLBACK_LOCALE=fa
APP_FAKER_LOCALE=fa_IR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis

CACHE_STORE=redis
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

PAYMENT_GATEWAY=zarinpal
ZARINPAL_MERCHANT_ID=
ZARINPAL_SANDBOX=true
ZARINPAL_CALLBACK_URL=/payment/callback
ZARINPAL_AMOUNT_UNIT=toman

TARA_SANDBOX=true
TARA_USERNAME=tara_ipg
TARA_PASSWORD=tara_ipg@123_$
TARA_SERVICE_ID=101
TARA_AMOUNT_UNIT=toman
TARA_CALLBACK_URL=/payment/callback/tara
TARA_CLIENT_IP=
TARA_DEFAULT_GROUP=1
TARA_DEFAULT_GROUP_TITLE=عمومی
TARA_BASE_URL=https://pay.tara360.ir/pay
TARA_SANDBOX_BASE_URL=https://stage-pay.tara360.ir/pay
TARA_REFUND_PRINCIPAL=ipg_refund
TARA_REFUND_PASSWORD=123456
TARA_REFUND_BASE_URL=https://club.tara-club.ir/club
TARA_SANDBOX_REFUND_BASE_URL=https://stage.tara-club.ir/club

# مهلت رزرو موجودی برای سفارش آنلاین پرداخت‌نشده (دقیقه)
SHOP_UNPAID_TTL_MINUTES=60

# برای سید ۲۰۰۰ محصول: SEED_HEAVY=true
SEED_HEAVY=false

# --- عملکرد فروشگاه (سرور: حتماً Redis) ---
# CACHE_STORE=redis
# SESSION_DRIVER=redis
# QUEUE_CONNECTION=redis
# SHOP_CACHE_HOME_TTL=600
# SHOP_CACHE_PRODUCTS_TTL=900
# SHOP_CACHE_CATEGORIES_TTL=3600
# بعد از deploy: php artisan shop:cache-warm && php artisan config:cache && php artisan route:cache

SMS_DRIVER=log
KAVENEGAR_API_KEY=
KAVENEGAR_SENDER=
KAVENEGAR_OTP_TEMPLATE=verify

ADMIN_PASSWORD=change-me

# --- بهینه‌سازی تصویر بعد از آپلود Filament (WebP + resize) ---
# IMAGE_OPTIMIZER_ENABLED=true
# IMAGE_OPTIMIZER_DRIVER=gd

# --- ذخیره‌سازی Runflare (volume پایدار /data — الزامی برای آپلود در چند پاد) ---
# FILESYSTEM_PUBLIC_ROOT=/data
# FILESYSTEM_PUBLIC_URL=https://fm-cgtg-foroshgahsaz.runflare.cloud/data
# LIVEWIRE_TEMP_DISK=livewire-tmp
# LIVEWIRE_TEMP_ROOT=/data
